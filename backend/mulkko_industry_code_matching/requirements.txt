# 물꼬 업종코드 매칭 - 런타임 의존성
# (개발 시 사용한 버전대. 백엔드 환경에 맞춰 조정 가능)
openai>=1.30
chromadb>=0.5
pandas>=2.0
python-dotenv>=1.0
numpy>=1.24

# 로컬 임베딩(bge-m3) 사용 시 필요. OpenAI 임베딩만 쓰면 생략 가능.
#   pip install torch --index-url https://download.pytorch.org/whl/cu124   (GPU)
torch>=2.2
sentence-transformers>=3.0
# FlagEmbedding>=1.2   # sentence-transformers 대신 쓸 경우
